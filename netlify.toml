[build]
  command = "prisma generate && prisma db push --accept-data-loss && node prisma/seed.js && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
